
function makeDisappear() {
  var punctuations = document.querySelectorAll('.disappear');
  punctuations.forEach((element) => {
    console.log(element)

    element.style.color = "#FF98D0"
  });
}

function makeComma() {
  var punctuations = document.querySelectorAll('.comma');
  punctuations.forEach((element) => {
    console.log(element)

    element.style.color = "#FF98D0"
  });
}

function makePeriod() {
  var punctuations = document.querySelectorAll('.period');
  punctuations.forEach((element) => {
    console.log(element)

    element.style.color = "#FF98D0"
  });
}